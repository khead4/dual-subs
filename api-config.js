// Set this to a hosted API origin when the frontend and backend live separately.
// Recommended: deploy the Docker backend to Cloud Run, then paste that HTTPS URL here.
// Example: window.DUAL_SUBTITLE_API_BASE = "https://dual-subs-api-abc123-uc.a.run.app";
window.DUAL_SUBTITLE_API_BASE = window.DUAL_SUBTITLE_API_BASE || "";
